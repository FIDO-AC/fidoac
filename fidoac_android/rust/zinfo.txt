In AndroidProjectroot (Not RustRoot):

Build
./gradlew cargoBuild

In rustroot ():
Build ServerBinary
cargo build --bins --release

cargo run --release sample_input.json verificationkey 

Run Tests
cargo test -- --nocapture


btoa(challenge).replaceAll("+","-").replaceAll("/","_")